import 'package:flutter/material.dart';

class AmbulenceService extends StatelessWidget {
  const AmbulenceService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Ambulence service")),
    );
  }
}
